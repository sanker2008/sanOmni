[build]
# Limit parallel jobs to reduce peak memory usage during compilation
jobs = 1

[profile.dev]
# Reduce memory usage per compilation unit
codegen-units = 512

[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-arg=/INCREMENTAL:NO"]
