# 2026-07-31

## Inserted batch2 China-geography photography templates into template library
- Source: shared report `report_2026-07-31_batch2.md` (10 photo analyses, items 11-20).
- Target DB: `D:\sanomnidata\data\database.sqlite`, table `prompt_groups` (the template library).
- Inserted 10 rows, category `Travel & Nature`, is_published=1, price 4.99.
- Each row = English Text-to-Image prompt + Negative prompt (verbatim from report), English name/description/tags; no Chinese anywhere (CJK scan = 0).
- Backup created: `database_before_batch2_china_geo_insert_20260731.sqlite`.
- Total prompt_groups: 110 -> 120.
- Lesson: DB edits via Python heredoc fail on apostrophes; write script to a file and run it instead.

## Updated the 10 batch2 templates: draft + aspect_ratio variable
- Per user request ("都需要"): set all 10 to draft (is_published=0, publish_status='draft'), and added an `aspect_ratio` select variable (default empty = no change; options 3:2/16:9/9:16/1:1/21:9/4:3/4:5).
- Placeholder `{{aspect_ratio}}` appended to both `prompt` and `template_schema.raw_prompt`.
- Category kept `Travel & Nature`.
- Whole-table CJK scan still 0 (no Chinese).
- Incremental backup: `database_before_draft_var_update_20260731.sqlite`.

## Enriched the 10 batch2 templates with proper variables (referenced existing library pattern)
- Mirrored the library convention: each template now has multiple `{{placeholder}}` in prompt + `combobox` variables (allow_custom=true, default=original wording, 2-3 options) + final `aspect_ratio` `select` (default `--ar 3:2`, 7 options). Appended `{{aspect_ratio}} --style raw`.
- Rebuilt both `prompt` column and `template_schema.raw_prompt` to the placeholder version; variables list rebuilt in `template_schema`.
- Verification: 0 placeholder/variable mismatches across the 10; whole-table CJK scan = 0.
- Backup: `database_before_var_enrich_20260731.sqlite`.
- Library var convention learned: combobox vars carry subject/lighting/optics; aspect_ratio select appended as `{{aspect_ratio}} --style raw` (or just `{{aspect_ratio}}` for some).

## Generated an image from the latest template (Everest) with default values
- Latest template by rowid = `Mount Everest North Face - Himalayan Peak`.
- Resolved default prompt (scene_subject/sky/lighting_optics defaults + aspect_ratio --ar 3:2) via ImageGen, size 1536x1024 (quality high).
- Saved: `D:\dev\san\sanOmni\generated-images\majestic_north_face_of_Mount_E_2026-07-31T12-22-37.png`.
- Note: ImageGen tool has no negative_prompt param, so template negative_prompt was not passed.

## Imported the Everest image into inbox + linked template + tagged
- Copied generated PNG into `D:\sanomnidata\inbox\openai\gpt-image-2\openai_gpt-image-2_20260731_majestic-north-face-of-mount-everest.png`.
- New `images` row: id `img_1e752b4452`, status=`inbox`, vendor/model `openai`/`gpt-image-2`, dims 1216x832, sha256 hash, prompt = template defaults rendered (scene_subject/sky/lighting_optics + --ar 3:2 --style raw).
- Linked to Everest template `942a9392-...` via `image_prompt_group_relations` (role=gallery, is_cover=1, variant_key='defaults', variant_json = the 4 default values).
- Tagged with 7 tags: landscape, nature, alpine-peaks, minimalist, travel (reused) + everest, himalaya (created). use_count incremented on reused tags.
- Backup: `database_before_inbox_import_20260731.sqlite`.

## Corrected vendor/model for the Everest image
- User pointed out usage record shows model = `hunyuan-image-v3.0-art`, not `openai`/`gpt-image-2`.
- Added vendor `tencent` + model `hunyuan-image-v3.0-art` to `vendors`/`models` tables (required `path`, `updated_at`, `version` columns).
- Moved file from `inbox/openai/gpt-image-2/...` to `inbox/tencent/hunyuan-image-v3.0-art/tencent_hunyuan-image-v3.0-art_20260731_majestic-north-face-of-mount-everest.png`.
- Updated `images` row: `storage_vendor_id=tencent`, `storage_model_id=hunyuan-image-v3.0-art`, `primary_model_id=hunyuan-image-v3.0-art`, plus path/filename fields.
- Updated relation caption to "Generated with Hunyuan Image v3.0 Art from the template defaults".
- Current vendor counts: OpenAI 81, Google 80, Tencent 1, Midjourney 0, Unknown 0.
- Backup: `database_before_vendor_fix_20260731.sqlite`.
